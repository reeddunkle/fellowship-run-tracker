export const ROUTES = {
  backgroundJobEvents: "/background-jobs/events",
  dungeonRunEvents: "/dungeon-run/events",
  liveSplitEvents: "/live-split/events",
  trackingEvents: "/tracking/events",
} as const;
